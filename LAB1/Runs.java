// Program Name			Runs.java// Course					CSE1302J// Name					   Travis Clement// Assignment				Lab 1// Due Date					5/20/2015// Purpose					Finds the length of the longest run of heads in 100 flips of a coin.// ********************************************************************// Runs.java//// Finds the length of the longest run of heads in 100 flips of a coin.// ********************************************************************import java.util.Scanner;public class Runs{    public static void main (String[] args)    {	final int FLIPS = 100; // number of coin flips	int currentRun = 0; // length of the current run of HEADS	int maxRun = 0;     // length of the maximum run so far   int lastface = 0;	Scanner scan = new Scanner(System.in);	Coin myCoin = new Coin();	// Flip the coin FLIPS times	for (int i = 0; i < FLIPS; i++)	    {		myCoin.flip();                  if (lastface != myCoin.getFace())      {        System.out.println();      }           if (myCoin.getFace() == 0)      {            currentRun++;      System.out.print(myCoin + " ");                  if (currentRun > maxRun)        {        maxRun = currentRun;        }      }      else      {      currentRun = 0;      System.out.print(myCoin + " ");      }          //reset the lastface       lastface = myCoin.getFace();			    }	// Print the results   System.out.println("\nThe maximum number of heads is: " + maxRun);   System.out.println ("Coded by Travis Clement");    }    }